{% extends 'baseBack.html.twig' %}

{% block title %}Créer un nouveau transfert{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="card shadow-lg border-0 rounded-4 p-4">

        <!-- 🏷️ Titre principal -->
        <h2 class="text-center text-primary fw-bold mb-4">
            🚗 Ajouter un nouveau trajet transfert
        </h2>

        <!-- 📋 Début du formulaire -->
        {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}

        <div class="row g-4">

            <!-- 🧱 Colonne gauche -->
            <div class="col-lg-6">

                <div class="mb-3">
                    {{ form_label(form.lieuDepart, 'Lieu de départ') }}
                    {{ form_widget(form.lieuDepart, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Aéroport Tunis-Carthage'}}) }}
                    {{ form_errors(form.lieuDepart) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.lieuArrivee, 'Lieu d’arrivée') }}
                    {{ form_widget(form.lieuArrivee, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Hammamet'}}) }}
                    {{ form_errors(form.lieuArrivee) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.prix, 'Prix (TND)') }}
                    {{ form_widget(form.prix, {'attr': {'class': 'form-control', 'placeholder': 'Ex: 120'}}) }}
                    {{ form_errors(form.prix) }}
                </div>

            </div>

            <!-- 🧱 Colonne droite -->
            <div class="col-lg-6">

                <div class="mb-3">
                    {{ form_label(form.duree, 'Durée estimée') }}
                    {{ form_widget(form.duree, {'attr': {'class': 'form-control', 'placeholder': 'Ex: 1h30'}}) }}
                    {{ form_errors(form.duree) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.image, 'Image du trajet (optionnelle)') }}
                    {{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.image) }}
                </div>

            </div>
        </div>

        <hr class="my-4">

        <!-- 🔘 Boutons -->
        <div class="text-center mt-4">
            <button class="btn btn-success px-4">
                💾 {{ button_label|default('Enregistrer') }}
            </button>
            <a href="{{ path('app_transfere_index') }}" class="btn btn-secondary px-4">
                ↩️ Retour à la liste
            </a>
        </div>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
