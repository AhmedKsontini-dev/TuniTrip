<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Voucher de Transfert - TuniTrip</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            color: #333;
            background: #f9f9f9;
            padding: 40px;
        }
        .voucher-container {
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            padding: 40px;
            border: 1px solid #ddd;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            position: relative;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #eee;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #d32f2f;
            text-transform: uppercase;
        }
        .voucher-title {
            font-size: 28px;
            text-transform: uppercase;
            color: #555;
        }
        .section {
            margin-bottom: 30px;
        }
        .section-title {
            font-size: 16px;
            font-weight: bold;
            text-transform: uppercase;
            border-bottom: 1px solid #eee;
            margin-bottom: 15px;
            padding-bottom: 5px;
            color: #777;
        }
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }
        .info-item {
            margin-bottom: 5px;
        }
        .label {
            font-weight: bold;
            color: #555;
        }
        .value {
            color: #333;
        }
        .highlight {
            font-size: 1.2em;
            color: #d32f2f;
            font-weight: bold;
        }
        /* Fake Stamp */
        .stamp {
            position: absolute;
            bottom: 100px;
            right: 50px;
            width: 150px;
            height: 150px;
            border: 5px solid #d32f2f; /* Red Ink */
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #d32f2f;
            font-weight: bold;
            font-size: 24px;
            text-transform: uppercase;
            transform: rotate(-15deg);
            opacity: 0.8;
            background: rgba(211, 47, 47, 0.05);
            box-shadow: inset 0 0 20px rgba(211, 47, 47, 0.2);
            z-index: 10;
        }
        .stamp-inner {
            text-align: center;
            line-height: 1.2;
            border: 2px solid #d32f2f;
            padding: 10px;
            width: 120px;
            height: 120px;
            border-radius: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .stamp small {
            font-size: 10px;
            display: block;
            margin-top: 5px;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #aaa;
            margin-top: 50px;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }
        .print-btn {
            display: block;
            width: 100%;
            max-width: 200px;
            margin: 20px auto;
            padding: 10px;
            background: #333;
            color: #fff;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
            border: none;
            border-radius: 5px;
        }
        @media print {
            .print-btn {
                display: none;
            }
            body {
                background: #fff;
                padding: 0;
            }
            .voucher-container {
                box-shadow: none;
                border: none;
            }
        }
    </style>
</head>
<body>

    <button onclick="window.print()" class="print-btn">🖨️ Imprimer le Voucher</button>

    <div class="voucher-container">
        <!-- Stamp -->
        <div class="stamp">
            <div class="stamp-inner">
                {% if reservation.statut == 'confirmee' %}
                    CONFIRMÉ
                {% elseif reservation.statut == 'annulee' %}
                    ANNULÉ
                {% else %}
                    EN ATTENTE
                {% endif %}
                <small>{{ "now"|date("d/m/Y") }}</small>
            </div>
        </div>

        <div class="header">
            <div class="logo">
                <img src="{{ asset('assetsFront/images/logo.png') }}" alt="TuniTrip" style="max-height: 50px; vertical-align: middle;">
            </div>
            <div class="voucher-title">
                Voucher #{{ reservation.id }}
            </div>
        </div>

        <div class="section">
            <div class="section-title">Informations Client</div>
            <div class="info-grid">
                <div class="info-item">
                    <span class="label">Nom Complet:</span>
                    <span class="value">{{ reservation.firstName }} {{ reservation.lastName }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Email:</span>
                    <span class="value">{{ reservation.email }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Téléphone:</span>
                    <span class="value">{{ reservation.tel }}</span>
                </div>
                <div class="info-item">
                    <span class="label">WhatsApp:</span>
                    <span class="value">{{ reservation.whatsappNumber }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Vol N°:</span>
                    <span class="value">{{ reservation.flightNumber ?: 'N/A' }}</span>
                </div>
            </div>
        </div>

        <div class="section">
            <div class="section-title">Détails du Transfert</div>
            <div class="info-grid">
                <div class="info-item">
                    <span class="label">Type:</span>
                    <span class="value">{{ reservation.transferType.value == 'return' ? 'Aller-Retour' : 'Aller simple' }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Passagers:</span>
                    <span class="value">{{ reservation.persons }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Départ:</span>
                    <span class="value">{{ reservation.trajetTransfert.lieuDepart }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Arrivée:</span>
                    <span class="value">{{ reservation.trajetTransfert.lieuArrivee }}</span>
                </div>
                
                <div class="info-item">
                    <span class="label">Date Pickup:</span>
                    <span class="value">
                        {{ reservation.pickupDate ? reservation.pickupDate|date('d/m/Y') : '-' }} 
                        à {{ reservation.pickupTime ? reservation.pickupTime|date('H:i') : '-' }}
                    </span>
                </div>
                <div class="info-item">
                    <span class="label">Lieu Pickup:</span>
                    <span class="value">{{ reservation.pickupLocation ?: 'N/A' }}</span>
                </div>
                <div class="info-item">
                    <span class="label">Lieu Dropoff:</span>
                    <span class="value">{{ reservation.dropoffLocation ?: 'N/A' }}</span>
                </div>

                {% if reservation.transferType.value == 'return' %}
                    <div class="info-item">
                        <span class="label">Date Retour:</span>
                        <span class="value">
                            {{ reservation.returnPickupDate ? reservation.returnPickupDate|date('d/m/Y') : '-' }} 
                            à {{ reservation.returnPickupTime ? reservation.returnPickupTime|date('H:i') : '-' }}
                        </span>
                    </div>
                {% endif %}

                <div class="info-item">
                    <span class="label">Prix Total:</span>
                    <span class="value highlight">{{ reservation.prixTotal }} DT</span>
                </div>
            </div>
        </div>
        
        {% if reservation.comments %}
        <div class="section">
             <div class="section-title">Commentaires</div>
             <p>{{ reservation.comments }}</p>
        </div>
        {% endif %}

        <div class="footer">
            Merci de choisir TuniTrip pour votre transfert.<br>
            Ceci est un document généré automatiquement.
        </div>
    </div>

</body>
</html>
